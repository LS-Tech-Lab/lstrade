{
  "functions": {
    "app.py": { "maxDuration": 45 }
  },
  "regions": ["dub1"],
  "crons": [
    { "path": "/api/cycle", "schedule": "0 12 * * *" }
  ],
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- . ':!dashboard' ':!*.md' ':!.github' ':!deploy'"
}
